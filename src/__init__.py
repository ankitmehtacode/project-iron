"""project-iron: CPU-only video-intelligence pipeline.

This module is intentionally empty of imports. ``import src`` must stay cheap
and dependency-free so that packaging checks, CI import smoke tests, and tools
like mypy can touch the package root without pulling in torch, OpenVINO, or
any other heavy optional dependency. Import submodules explicitly instead.
"""

__version__ = "0.1.0"
