# Core ML
torch==2.2.0 --index-url https://download.pytorch.org/whl/cpu
torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cpu

# OpenVINO (Intel edge inference)
openvino==2024.6.0

# LLM / Agentic
langchain==0.1.0
langchain-community==0.0.10

# Vector Store
faiss-cpu==1.9.0

# Embeddings / Transformers
tokenizers==0.21.0
sentence-transformers==3.0.0
huggingface-hub==0.26.0

# MLflow Tracking
mlflow==2.9.2
boto3==1.34.0

# Utilities
numpy==1.26.2
pandas==2.1.4
opencv-python-headless==4.8.1.78
Pillow==10.1.0
tqdm==4.66.1
python-dotenv==1.0.0
pyarrow==18.1.0
scikit-learn>=1.3.0

# Configuration (src/config.py)
pydantic==2.5.3
pydantic-settings>=2.2.0
PyYAML>=6.0

# Endurance harness (src/endurance/)
# psutil was already imported by endurance_run.py but never declared — the
# soak test could not be run from a clean install of this file.
psutil>=5.9.0
# scipy supplies the linear regression and Student's t used by the leak gate.
scipy>=1.11.0

# Dev / Linting / Testing
black==23.12.1
flake8==7.0.0
pre-commit==3.6.0
pytest==7.4.3
pytest-cov>=4.1.0
mypy>=1.8.0
# Property-based tests for the contract layer.
hypothesis>=6.98.0
# Stubs, so mypy --strict has something to check these against.
types-psutil
types-PyYAML
scipy-stubs
