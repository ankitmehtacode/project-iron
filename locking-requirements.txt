# Core ML
torch==2.2.0 --index-url https://download.pytorch.org/whl/cpu
torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cpu

# OpenVINO (Intel edge inference)
openvino==2024.6.0

# LLM / Agentic
langchain==0.1.0
langchain-community==0.0.10

# Vector Store
faiss-cpu==1.9.0

# Embeddings / Transformers
tokenizers==0.21.0
sentence-transformers==3.0.0
huggingface-hub==0.26.0

# MLflow Tracking
#
# mlflow==2.9.2 is REMOVED, not downgraded. It pinned pyarrow<15 while this
# file also pins pyarrow==18.1.0, so the two constraints were unsatisfiable and
# `pip install -r locking-requirements.txt` failed with ResolutionImpossible on
# every machine and every Python version — the "locking" file had never once
# installed. Removing this single line resolves the whole file with every other
# pin untouched (verified by dry run: torch 2.2.0, openvino 2024.6.0,
# numpy 1.26.2, opencv 4.8.1.78, pyarrow 18.1.0 all install together).
#
# It is safe to remove because nothing imports it: `grep -rn mlflow --include=*.py`
# matches zero files. It was an intention recorded in a requirements file, and
# that intention was blocking the entire install.
#
# Restoring experiment tracking means either pinning pyarrow<15 — which changes
# the library the event schema and Parquet writer run on, and is a measured
# change — or adopting a tracker that does not constrain pyarrow.
boto3==1.34.0

# Utilities
numpy==1.26.2
pandas==2.1.4
opencv-python-headless==4.8.1.78
Pillow==10.1.0
tqdm==4.66.1
python-dotenv==1.0.0
pyarrow==18.1.0
scikit-learn>=1.3.0

# Configuration (src/config.py)
pydantic==2.5.3
pydantic-settings>=2.2.0
PyYAML>=6.0

# Endurance harness (src/endurance/)
# psutil was already imported by endurance_run.py but never declared — the
# soak test could not be run from a clean install of this file.
psutil>=5.9.0
# scipy supplies the linear regression and Student's t used by the leak gate.
scipy>=1.11.0

# Point tracking (CoTracker3).
#
# Facebook Research publishes no PyPI wheel — the package is `pip install -e .`
# from source only, so the pin here is a git URL fixed to a specific commit
# SHA. `git+https://…@<sha>` is the deterministic form; without the @SHA a
# fresh clone would silently pull whichever main is HEAD on the day.
#
# LICENSE, and why this is dev-only: the majority of CoTracker is CC-BY-NC 4.0,
# with subcomponents under MIT (Particle Video Revisited) and Apache-2.0
# (TAP-Vid, LocoTrack). CC-BY-NC blocks commercial use of the code AND the
# `scaled_offline.pth` checkpoint. That is fine for evaluation and internal
# capability measurement — this is what the pin authorises — and the ship
# question stays open in the dataset/model registry rather than being decided
# here. Any product build that includes tracker inference must resolve the
# licence before it goes out.
#
# The Day-10 report notes that `scaled_offline.pth` is bidirectional (looks
# at future frames), which makes it an oracle for offline scoring but not the
# production path — the streaming penalty from the online checkpoint is
# unmeasured. That number is a Day-12+ item; the pin here does not settle it.
#
# Deps NOT declared by CoTracker's setup.py but which its imports need at
# runtime: torch and torch.nn.functional (already pinned above), plus einops
# for tensor reshapes inside `cotracker.models.core`. The runtime path used by
# the eval script — `cotracker.predictor.CoTrackerPredictor` — pulls only
# torch + einops; the training/dataset paths need more (imageio-ffmpeg,
# gluefactory, etc.) and are not exercised here.
cotracker @ git+https://github.com/facebookresearch/co-tracker.git@82e02e8029753ad4ef13cf06be7f4fc5facdda4d
einops>=0.7.0

# Dev / Linting / Testing
black==23.12.1
flake8==7.0.0
pre-commit==3.6.0
pytest==7.4.3
pytest-cov>=4.1.0
mypy>=1.8.0
# Property-based tests for the contract layer.
hypothesis>=6.98.0
# Stubs, so mypy --strict has something to check these against.
types-psutil
types-PyYAML
scipy-stubs
